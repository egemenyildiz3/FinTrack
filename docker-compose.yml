services:
  backend:
    build:
      context: ./backend/FinTrack.Api
    container_name: fintrack-backend
    volumes:
      - fintrack-data:/data
      - ${EXTRA_BACKUP_PATH:-/tmp/fintrack-backup-unused}:/mnt/extra-backups
    environment:
      - EXTRA_BACKUP_PATH=${EXTRA_BACKUP_PATH:+/mnt/extra-backups}
    expose:
      - "8080"
    ports:
      - "5099:8080"
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
    container_name: fintrack-frontend
    depends_on:
      - backend
    ports:
      - "8080:80"
    restart: unless-stopped

volumes:
  fintrack-data:
