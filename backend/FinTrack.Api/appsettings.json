{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  // Optional extra directory to write monthly backups to (in addition to the volume).
  // Override at runtime with the EXTRA_BACKUP_PATH environment variable.
  // Leave empty or omit to disable.
  "ExtraBackupPath": ""
}
